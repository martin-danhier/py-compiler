fun main()
{
   # Regardez vite fait les dernières lignes quand on passera sur l'autre
   # console
   display("Yay, trying to work with arguments");
   var navire : string;
   navire = "costa concordia";
   var moto : string;
   moto = "pcg600";
   test(navire, moto);
}

fun test(string bateau, string test)
{
   display(bateau)
   display(test)
}
